appId: com.wraeclastledger.app
productName: WraeclastLedger

publish:
  provider: github
  owner: gund0lf
  repo: wraeclastledger_react
  private: false   # public repo — electron-updater can check releases without a token
  releaseType: release

win:
  target:
    - target: nsis
      arch: [x64]

linux:
  target:
    - target: AppImage
      arch: [x64]
  category: Utility
  executableName: wraeclastledger
  syncDesktopName: true
  icon: resources/icon.png
  extraResources:
    - from: resources/linux/wl-proton-clipboard.exe
      to: linux/wl-proton-clipboard.exe

appImage:
  artifactName: ${productName}-${version}-${arch}.${ext}

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  deleteAppDataOnUninstall: false
  shortcutName: WraeclastLedger

files:
  - out/**/*
  - "!out/**/*.map"

directories:
  output: dist
  buildResources: resources
